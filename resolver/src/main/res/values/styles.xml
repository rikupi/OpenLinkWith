<?xml version="1.0" encoding="utf-8"?>
<resources>

  <style name="BottomSheet.Light" parent="Base.Theme.OpenWith">
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowNoTitle">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:backgroundDimEnabled">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:windowTranslucentStatus">false</item>
    <item name="android:windowTranslucentNavigation">false</item>
    <item name="listPreferredItemPaddingLeft">?attr/dialogPreferredPadding</item>
    <item name="listPreferredItemPaddingRight">?attr/dialogPreferredPadding</item>
    <item name="colorControlActivated">?attr/colorAccent</item>
    <!-- OneUI pill-style buttons -->
    <item name="buttonBarPositiveButtonStyle">@style/Widget.OneUI.Button.Positive</item>
    <item name="buttonBarNegativeButtonStyle">@style/Widget.OneUI.Button.Negative</item>
  </style>

  <!-- OneUI: filled neutral pill button (Always) -->
  <style name="Widget.OneUI.Button.Positive" parent="Widget.MaterialComponents.Button">
    <item name="android:textColor">@color/oneui_card_background</item>
    <item name="backgroundTint">@color/oneui_primary_text</item>
    <item name="cornerRadius">50dp</item>
    <item name="android:minWidth">88dp</item>
    <item name="android:paddingStart">20dp</item>
    <item name="android:paddingEnd">20dp</item>
  </style>

  <!-- OneUI: outlined neutral pill button (Just Once) -->
  <style name="Widget.OneUI.Button.Negative" parent="Widget.MaterialComponents.Button.OutlinedButton">
    <item name="android:textColor">@color/oneui_primary_text</item>
    <item name="strokeColor">@color/oneui_secondary_text</item>
    <item name="cornerRadius">50dp</item>
    <item name="android:minWidth">88dp</item>
    <item name="android:paddingStart">20dp</item>
    <item name="android:paddingEnd">20dp</item>
  </style>

</resources>
